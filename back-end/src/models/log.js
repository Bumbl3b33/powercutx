class Log {
  constructor(logId, DistrictId, LogDate, LogTime) {
    this.logId = logId;
    this.DistrictId = DistrictId;
    this.LogDate = LogDate;
    this.LogTime = LogTime;
  }
}

module.eports = Log;
